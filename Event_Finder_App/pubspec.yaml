name: Event_Finder_App
description: Mobile Major Group Project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

flutter_icons:
  android: true
  image_path: "assets/img/icon.png"

dependencies:
  gradient_app_bar: ^0.0.1 #added by tharuni for gradience
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^0.1.3

  image_picker: #added by Tharuni for image uploading
  firebase_storage: #added by Tharuni for image uploading
  firebase_messaging: #added by Tharuni for push notifications
  flutter_local_notifications:
  auto_size_text: ^2.1.0

  timezone:

dev_dependencies:
  flutter_test:
    sdk: flutter
  cloud_firestore: "^0.14.3"
  firebase_auth: "^0.18.3"
  provider:
  flip_card: ^0.4.4
  sqflite:
  path:
  bubble_tab_indicator: "^0.1.4"
  intl: ^0.16.1
  google_maps_flutter:
  latlng:
  geolocator:
  google_maps_place_picker:
  geocoding:
  scroll_to_index:
  scrollable_positioned_list:
  date_format:
  flushbar:
  introduction_screen: ^1.0.7
  flutter_launcher_icons:

flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/img/
    - assets/gifs/
    - assets/img/profile.png
    - assets/img/competition.png
  #   - images/a_dot_burr.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
